# URL of your outbound service (the Fastify server that handles Twilio + ElevenLabs).
# Required for "Start Training" to place calls.
#
# Local dev: run the outbound service (e.g. node outbound.js) and expose it via a tunnel
# (e.g. ngrok: https://xxxx.ngrok.io). Then set:
# OUTBOUND_CALL_API_URL=https://xxxx.ngrok.io
#
# No trailing slash.
OUTBOUND_CALL_API_URL=
